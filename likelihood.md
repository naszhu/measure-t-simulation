define the likelihood **as a function of θ given data x**:

$$L(θ;x)=P(x∣θ)$$

Even if θ is unknown-but-fixed, you can compute how likely your data would be if θ had different values.

[[maximum likelihood estimator (MLE)]]