[[Bayesian Statistics]]

Real world process:
2. **Data generation process**:
    
    - True: P_X(A) = P(X⁻¹(A)) requires P
        
    - But in practice: We **estimate P_X directly** from data without knowing P  
        Example: Compute sample mean μ̂ from {x₁,...,xₙ} to estimate P_X's μ
        
3. **Bayesian parameter space**:

4. **State space vs. data**:
    
    - 𝒳 = set of possible data values (e.g., ℝ for heights)
        
    - **Data** = realized values {x₁,...,xₙ} ⊂ 𝒳